name: 'Setup CI Step'
description: 'Checkout PR and use same node version for jobs'

runs:
  using: 'composite'

  steps:
    - name: Use Node.js 24
      uses: actions/setup-node@v6
      with:
        node-version: 24
