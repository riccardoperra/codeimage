import {createTheme} from '../../core';
import {%%THEME_NAME%%} from './%%THEME_NAME%%';

export const %%THEME_NAME%%Theme = createTheme({
  id: '%%THEME_NAME%%',
  editorTheme: %%THEME_NAME%%,
  properties: {
    darkMode: false,
    label: '',
    previewBackground: '',
    terminal: {
      main: '',
      text: '',
    },
  },
});
